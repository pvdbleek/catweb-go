apiVersion: apps/v1
kind: Deployment
metadata:
  name: catweb-go
spec:
  selector:
    matchLabels:
      app: catweb
  replicas: 6
  template:
    metadata:
      labels:
        app: catweb
    spec:
      containers:
      - name: catweb
        image: pvdbleek/catweb-go
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: catweb
  labels:
    app: catweb
spec:
  type: NodePort
  ports:
    - port: 8080
      nodePort: 32090
  selector:
    app: catweb
